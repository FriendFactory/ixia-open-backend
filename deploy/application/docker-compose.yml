# To successfully run load environment variables from .deploy/.env file
# That file is generated by terraform due creating environment
version: "3"
services:
  appservice:
    image: ${APPSERVICE_REPOSITORY_URL}:${RELEASE:-latest}
    build:
      context: ../..
      dockerfile: ./deploy/application/appservice.Dockerfile
